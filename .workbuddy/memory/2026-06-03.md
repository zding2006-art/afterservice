## 2026-06-03 售后数据分析系统 V1.1 多语言+部署准备

### 完成内容
1. **前端 i18n 完整集成**：index.html 全部重写 — 加入登录覆盖层、语言切换按钮（中/EN/日）、data-i18n 属性、auth 检查、credentials 传递、lang 参数传递
2. **i18n.js 补充**：添加 comparison 部分 12 个缺失的 i18n key（三语）
3. **app.py 部署优化**：CORS 支持环境变量 `CORS_ORIGINS`、端口支持 `PORT` 环境变量
4. **部署包 `deploy/`**：精简项目结构，仅包含运行必需文件（backend 核心 + frontend + start.sh + README_JP.md）
5. **日文 README**：完整部署指南（Ubuntu 上从零开始，含 Nginx、HTTPS、セキュリティ設定）
6. **密码**：默认 `waterx2026`，通过 `SHOUHOU_PASSWORD` 环境变量修改
