## 2026-06-05 GitHub 发布 + insights 多语言改造

### insights.py 多语言改造
- `backend/insights.py` 完整重写，所有分析文案改为三语言字典 `I18N_INSIGHTS`（zh/en/ja）
- `generate_insights(data, lang='zh')` 接受 lang 参数
- `app.py` `/api/analysis/insights` 接受 `?lang=zh|en|ja` 查询参数

### 前端适配
- `frontend/i18n.js` 新增 `getLang()` 方法，映射 `zh-CN` → `zh`
- `frontend/index.html` `loadAIInsights()` 调用 API 时带 `lang` 参数
- deploy/frontend 已同步

### GitHub 发布
- 仓库: https://github.com/zding2006-art/afterservice
- 已创建 .gitignore，排除 dist/build/*.db/uploads/
- master 分支已推送成功
